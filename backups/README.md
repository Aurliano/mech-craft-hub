# Database backups

Place emergency dumps here when downloading from Liara:

```bash
liara shell --app mech-craft-hub-main
# after backup_legacy_sayda_db.sh on server:
# copy emergency.dump to local via Liara file manager or scp if available
```

Production path on Liara: `/app/backups/emergency.dump`
