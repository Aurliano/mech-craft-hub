"""Settings package for environment-specific Django configuration."""

# This package allows using config.settings.dev and config.settings.prod
# For backward compatibility with config.settings, the main settings.py file
# is used directly (not imported here to avoid circular imports)

